# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: discipline/discipline.spec.ts >> discipline: layout padding + list refreshes after creation
- Location: tests/discipline/discipline.spec.ts:3:1

# Error details

```
Error: expect(locator).not.toBeVisible() failed

Locator:  locator('.modal-card')
Expected: not visible
Received: visible
Timeout:  5000ms

Call log:
  - Expect "not toBeVisible" with timeout 5000ms
  - waiting for locator('.modal-card')
    14 × locator resolved to <div class="modal-card">…</div>
       - unexpected value "visible"

```

```yaml
- button "Close": ×
- heading "New discipline" [level=1]
- text: Name
- textbox "Name":
  - /placeholder: e.g. Badminton
  - text: Test Discipline
- text: Short name (badges)
- textbox "Short name (badges)":
  - /placeholder: e.g. BDMT
- text: Min team size
- spinbutton "Min team size": "5"
- text: Max team size (empty = unlimited)
- spinbutton "Max team size (empty = unlimited)"
- checkbox "Roles are required on every team (hard coverage)"
- text: Roles are required on every team (hard coverage) Roles
- paragraph: No roles yet.
- button "+ Add role"
- text: Attributes (rated per capability)
- paragraph: No attributes yet.
- button "+ Add attribute"
- paragraph: At least one attribute is required.
- button "Cancel"
- button "Add discipline"
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test("discipline: layout padding + list refreshes after creation", async ({ page }) => {
  4  |   await page.goto("http://localhost:4173/");
  5  |   await expect(page.locator(".app")).toBeVisible({ timeout: 15000 });
  6  | 
  7  |   // Navigate to Disciplines via bottom nav (last item)
  8  |   await page.locator(".bottom-nav .nav-link").last().click();
  9  |   await expect(page.locator(".screen h1")).toHaveText("Disciplines");
  10 | 
  11 |   // 1. .screen wrapper has padding
  12 |   const screen = page.locator(".screen");
  13 |   await expect(screen).toBeVisible();
  14 |   const screenPadding = await screen.evaluate((el) => {
  15 |     const cs = window.getComputedStyle(el);
  16 |     return { top: parseInt(cs.paddingTop), left: parseInt(cs.paddingLeft) };
  17 |   });
  18 |   expect(screenPadding.top).toBeGreaterThanOrEqual(12);
  19 |   expect(screenPadding.left).toBeGreaterThanOrEqual(12);
  20 | 
  21 |   // 2. Count initial disciplines
  22 |   const initialRows = await page.locator(".roster .row").count();
  23 |   expect(initialRows).toBeGreaterThanOrEqual(2);
  24 | 
  25 |   // 3. Open new discipline modal
  26 |   await page.getByRole("button", { name: /New Discipline/ }).click();
  27 |   const modal = page.locator(".modal-card");
  28 |   await expect(modal).toBeVisible();
  29 | 
  30 |   // 4. Fill name
  31 |   await modal.getByRole("textbox", { name: "Name", exact: true }).fill("Test Discipline");
  32 | 
  33 |   // Uncheck "roles required" since we don't add roles
  34 |   await modal.getByRole("checkbox", { name: /Roles are required/ }).uncheck();
  35 | 
  36 |   // 6. Save
  37 |   const saveBtn = modal.getByRole("button", { name: /Add discipline/ });
  38 |   await expect(saveBtn).toBeEnabled({ timeout: 3000 });
  39 |   await saveBtn.click();
  40 | 
  41 |   // 7. Modal closes
> 42 |   await expect(modal).not.toBeVisible({ timeout: 5000 });
     |                           ^ Error: expect(locator).not.toBeVisible() failed
  43 | 
  44 |   // 8. New discipline appears WITHOUT page refresh
  45 |   await expect(page.locator(".roster .row")).toHaveCount(initialRows + 1, { timeout: 5000 });
  46 |   await expect(page.getByText("Test Discipline")).toBeVisible();
  47 | });
  48 | 
```